# Required: 12-word mnemonic phrase for wallet derivation
# This mnemonic is used to derive both EVM and Cosmos addresses
MNEMONIC="your twelve word mnemonic phrase here"

# Optional: Server port (default: 8088)
PORT=8088

# Optional: Server host (default: 0.0.0.0)
HOST=0.0.0.0

# Optional: Node environment (development or production)
NODE_ENV=development

# Optional: WalletConnect/Reown project ID for EVM wallet connections
# Get one at https://cloud.reown.com/
VITE_REOWN_PROJECT_ID=your_reown_project_id_here
